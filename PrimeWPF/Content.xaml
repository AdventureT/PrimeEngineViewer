<Window x:Class="PrimeWPF.Content"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrimeWPF"
        mc:Ignorable="d"
        Title="Content" Height="450" Width="800">
    <Grid Margin="0,0,532,0">
        <TreeView x:Name="treeView" SelectedItemChanged="treeView_SelectedItemChanged"/>
        <Grid Margin="273,0,-529,0" HorizontalAlignment="Center">
            <ScrollBar Name="vscroll" 
            Grid.Row="0"
            Orientation="Vertical"  
            Minimum="-180" Maximum="180" 
            LargeChange="10" SmallChange="1" Value="0" Margin="497,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Height="397" />

            <ScrollBar Name="hscroll" Grid.Column="0"
            Orientation="Horizontal" 
            Minimum="-180" Maximum="180" 
            LargeChange="10" SmallChange="1" Value="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="421" Margin="39,0,0,10" />
            <ScrollBar Name="zscroll" Grid.Column="0"
            Orientation="Horizontal" 
            Minimum="-180" Maximum="180" 
            LargeChange="10" SmallChange="1" Value="0" Margin="0,10,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" Width="436" />
            <!-- Viewport3D is the rendering surface. -->
            <Viewport3D Name="myViewport" Height="434" VerticalAlignment="Top" >

                <!-- Add a camera. -->
                <Viewport3D.Camera>
                    <PerspectiveCamera 
                        FarPlaneDistance="20" 
                        LookDirection="0,0,1" 
                        UpDirection="0,1,0" 
                        NearPlaneDistance="1" 
                        Position="0,0,-15" 
                        FieldOfView="90" >
                        <PerspectiveCamera.Transform>
                            <Transform3DGroup>
                                <RotateTransform3D>
                                    <RotateTransform3D.Rotation>
                                        <AxisAngleRotation3D
                                      Axis="0 1 0" 
                                      Angle="{Binding ElementName=hscroll, Path=Value}" />
                                    </RotateTransform3D.Rotation>
                                </RotateTransform3D>
                                <RotateTransform3D>
                                    <RotateTransform3D.Rotation>
                                        <AxisAngleRotation3D
                                      Axis="1 0 0" 
                                      Angle="{Binding ElementName=vscroll, Path=Value}" />
                                    </RotateTransform3D.Rotation>
                                </RotateTransform3D>
                                <ScaleTransform3D
                                    ScaleX="{Binding ElementName=zscroll, Path=Value}" 
                                    ScaleY="{Binding ElementName=zscroll, Path=Value}"
                                    ScaleZ="{Binding ElementName=zscroll, Path=Value}"/>
                            </Transform3DGroup>
                        </PerspectiveCamera.Transform>
                    </PerspectiveCamera>

                </Viewport3D.Camera>



            </Viewport3D>

        </Grid>
    </Grid>


</Window>
